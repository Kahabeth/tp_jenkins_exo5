pipeline {
    agent any
        stages {
            stage('Docker build') {
                steps {
                    sh 'docker build -t 57.128.115.80:latest - < Dockerfile'
                }
            }
        }
}
